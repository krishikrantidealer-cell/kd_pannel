import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future<void> setupServiceLocator() async {
  // Navigation Service or other dependencies can be registered here
  // Example: sl.registerLazySingleton(() => NavigationService());
}
